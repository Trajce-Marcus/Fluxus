// Layout types moved to @fluxus/page-runtime (the renderer needs them);
// re-exported here so the editor keeps its local import paths.
export type { Panel, BorderSide, LayoutDefinition } from '@fluxus/page-runtime';
